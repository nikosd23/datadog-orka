{
  "name": "datadog-orka",
  "version": "1.0.0",
  "description": "Project to test orka datadog integration",
  "main": "app.js",
  "scripts": {
    "start": "node --max_old_space_size=${MAX_OLD_SPACE_SIZE:-384} app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "lint-staged": {
    "*.js": [
      "prettier --print-width 120 --single-quote true --write",
      "git add"
    ]
  },
  "keywords": [
    "datadog",
    "orka"
  ],
  "author": "Nikolaos Dimos",
  "license": "ISC",
  "dependencies": {
    "@workablehr/orka": "^0.33.8",
    "dd-trace": "^0.26.1"
  }
}
